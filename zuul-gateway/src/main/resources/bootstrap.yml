spring:
  cloud:
    config:
      name: zuul-gateway
      uri: http://localhost:8010
eureka:
  client:
    healthcheck:
      enabled: true
security:
  oauth2:
    resource:
      jwt:
        key-value: token-key
